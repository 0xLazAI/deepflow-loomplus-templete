# loom+ v0 / MVP 领域建模文档

## 1\. 文档目的

本文面向团队内部，用于定义 `loom+` 在当前阶段的领域边界、基线框架和最小验证路径。

当前任务聚焦在三件事：

- 复用 `Deepflow` 已验证过的底座能力
- 把“团队协作”这个领域收敛成可运行的框架
- 为后续开发、调试、升级和重新部署提供统一基线

本文不承担对外产品介绍和市场叙事职能。

---

## 2\. 一句话定义

**`loom+` 是一个复用 `Deepflow` 底座能力的团队协作框架。它接住协作意图，生成协调实例，推进状态流，输出工件，并在日常使用中逐步学习团队自己的协作方式。**

---

## 3\. 当前阶段的问题定义

`loom+` 当前阶段处理的核心问题是：

- 团队成员在聊天、任务、文档、会议和提醒之间反复切换
- 协作推进高度依赖人类手动路由上下文
- 状态变化、广播、跟进和回执分散在不同工具中
- 老需求能跑起来，新需求一改就容易失稳
- 早期“AI 员工”式通用聊天能力无法自然收敛成稳定协作系统

`loom+` 的探索重点放在：

- 如何把协作意图收进一个稳定框架
- 如何用规则、gate、工件和状态流替代散乱的聊天推进
- 如何让框架在变更需求后仍然可部署、可升级、可维护

---

## 4\. 与 Deepflow 的关系

`loom+` 复用 `Deepflow` 的底座能力，团队协作领域的具体流程需要重新建模。

### 4.1 当前可复用的底座能力

- 以实例为单位推进
- 显式状态流
- 工件沉淀
- 角色分工
- 人类 gate
- harness 约束
- 调试 / 回放 / 重跑
- 需求变化后的重新部署能力

### 4.2 团队协作场景里的重做内容

- 协作动作的对象模型
- 广播 / 私聊 / 局部展开的路由规则
- 协作回执和状态快照的输出方式
- 团队级与个人级协作偏好记忆
- 哪些动作需要确认，哪些动作可以直接执行

一句话：

`Deepflow` 给 `loom+` 的是底座能力。  
`loom+` 需要自己完成团队协作领域的建模。

---

## 5\. 基线版本的目标

`loom+ v0` 当前阶段不以“完整协作软件”为目标。

`loom+ v0` 要先做到以下几点：

- 团队协作意图可以进入系统
- 系统能把意图收敛成一个可追踪的协调实例
- 协调实例能沿着固定状态流推进
- 关键步骤会留下工件和回执
- 团队成员能感受到“少切工具、少找状态、少做路由”
- 框架能在新增需求后继续修改和重新部署

---

## 6\. loom+ 的基线框架

`loom+` 的基线框架分成两层：

### 6.1 固定层

固定层是每一个 `loom+` 实例都要共享的骨架。

固定层包含：

- 协调实例
- 协作状态机
- 工件系统
- 人类 gate
- 路由规则
- 记忆更新规则

### 6.2 可变层

可变层是不同团队通过使用逐步长出来的内容。

可变层包含：

- 团队协作习惯
- 个人偏好
- 广播风格
- 快照模板
- 提醒阈值
- 常用流程模板

固定层解决稳定和可部署。  
可变层负责长成“这个团队自己的 `loom+`”。

---

## 7\. 最小运行单位

`loom+` 以 **协调实例（Coordination Issue）** 作为最小运行单位。

### 7.1 定义

一次协作意图进入系统后，只要它会触发状态变化、路由、提醒、广播、摘要、确认或记录动作，就创建一个 `Coordination Issue`。

### 7.2 典型来源

- 启动一个跨职能任务
- 跟进一个热点响应
- 查询某个项目状态
- 从会议结果生成后续动作
- 改动某个负责人或 deadline
- 生成团队或个人简报

### 7.3 最小字段

- `id`
- `type`
- `source_input`
- `initiator`
- `current_status`
- `affected_scope`
- `required_gates`
- `created_at`
- `updated_at`

### 7.4 建议的 type

- `coordination_start`
- `task_dispatch`
- `status_request`
- `status_update`
- `meeting_followup`
- `decision_broadcast`
- `brief_generation`
- `reminder_trigger`

---

## 8\. 四个核心动作

`loom+` 基线版本可以先收敛成四个核心动作：

### 8.1 Ingest

系统接住外部意图和输入。

输入来源包括：

- 用户一句话
- 会议结果
- 外部报警
- 某个任务状态变化
- 某个文档更新

### 8.2 Route

系统判断：

- 该创建什么协调实例
- 该通知谁
- 该发在公海还是私聊
- 该生成什么类型的卡片或摘要
- 该不该先停下来等确认

### 8.3 Render

系统按需输出当前最合适的界面或摘要。

输出形式包括：

- 卡片
- 快照
- 摘要
- 回执
- 风险提示

### 8.4 Learn

系统把本次协作中的规则、偏好和确认结果写回记忆层。

写回内容包括：

- 团队级协作规则
- 个人级偏好
- 输出形式偏好
- 提醒节奏
- 广播习惯

---

## 9\. 基线状态流

每一个 `Coordination Issue` 沿着固定状态推进。

建议基线状态如下：

- `received`
- `parsed`
- `planned`
- `routed`
- `pending_confirmation`
- `executing`
- `receipted`
- `summarized`
- `closed`

### 9.1 状态含义

`received` : 系统刚收到一条协作意图

`parsed` : 意图已经被识别出类型和作用范围

`planned` : 系统已经形成一个最小执行计划

`routed` : 系统已经完成通知和职责分发

`pending_confirmation` : 当前动作等待人类确认

`executing` : 动作正在推进

`receipted` : 执行结果已经留下回执

`summarized` : 当前实例已经形成摘要或快照输出

`closed` : 当前实例完成或归档

---

## 10\. 基线工件

`loom+` 当前阶段先保留最少工件。

### 10.1 每个协调实例下的最小工件

- `intent-brief`
- `coordination-card`
- `routing-receipt`
- `status-snapshot`
- `execution-receipt`
- `summary-digest`
- `rule-update`

### 10.2 工件说明

`intent-brief` : 对输入意图的结构化理解

`coordination-card` : 当前实例的核心信息卡

`routing-receipt` : 系统已通知谁、已派发给谁

`status-snapshot` : 当前状态总览，按需生成

`execution-receipt` : 某次状态变化、提醒、更新、创建动作的确认结果

`summary-digest` : 给人类看的压缩摘要

`rule-update` : 新写入的团队规则或个人偏好

---

## 11\. Human-in-the-Loop 设计

`loom+` 在协作场景中也需要明确的人类 gate。

### 11.1 当前阶段必须保留的人类 gate

- 建任务确认
- 改 owner 确认
- 改 deadline 确认
- 发群公告确认
- 固定会议 action items
- 固定 decision
- 修改团队级规则

### 11.2 gate 的作用

这些 gate 决定了框架能否保持稳定。

协作系统允许 agent 做大量路由、提醒、聚合和总结。  
影响组织运行的关键动作仍然要由人确认。

---

## 12\. 稳定性约束

这一部分是 `loom+` 的核心。

### 12.1 单实例推进

任何重要协作动作都要挂在某个 `Coordination Issue` 下推进。

### 12.2 单一状态源

状态变化要写入系统工件或数据库，不允许只留在聊天文字中。

### 12.3 动作必须有回执

建任务、发提醒、改状态、改排期、发广播都需要 receipt。

### 12.4 广播必须克制

大群只承载：

- 结论
- 公示
- 风险
- 需要多人知晓的状态变化

中间过程默认进入私聊或局部展开区域。

### 12.5 关键动作必须卡 gate

影响团队协作结构的动作统一卡 gate，不允许静默执行。

### 12.6 可重跑

每个阶段需要支持单独重跑。

### 12.7 可回放

开发和团队成员需要看得见：

- 意图怎么被解析
- 系统做了哪些路由
- 最后发给了谁
- 哪一步停住了

### 12.8 记忆更新有边界

规则和偏好进入记忆层，需要有明确来源：

- 用户明确说过
- 用户确认过
- 重复行为稳定出现过

---

## 13\. 运行形态

`loom+` 的前台感受，应当围绕“协作意图进入系统后发生了什么”展开。

### 13.1 入口

当前阶段建议保留两个主要入口：

- `意图入口`
  
  - 团队成员说一句话，发起一次协作
- `状态入口`
  
  - 团队成员问一句话，查看某个范围内的当前状态

### 13.2 输出

输出优先级建议如下：

1. 先给最短结论
2. 再给卡片或快照
3. 需要确认时再附带交互动作
4. 复杂中间过程尽量留在局部或私聊

---

## 14\. 需求模板 A：热点响应协作流

### 14.1 场景

`crypto AI` 团队发现一个值得快速跟进的热点，需要在短时间内组织内容、视觉和定调。

### 14.2 用户输入示例

`@loom 今晚跟进这个热点，明天上午前给我一版英文 thread、一版中文解读、一张封面图，Jerry 写内容，Tom 做图，我来定调。`

### 14.3 系统流程

#### Step 1\. Ingest

系统接住热点响应意图。

#### Step 2\. Parse

系统识别：

- 这是一次热点响应协作
- 涉及内容、视觉、owner、deadline

#### Step 3\. 建立协调实例

创建一个 `Coordination Issue`：

- `type = coordination_start`
- `scope = hot-response`
- `owner = initiator`
- `assignees = Jerry / Tom`

#### Step 4\. 生成协调卡

系统输出 `coordination-card`：

- 本次目标
- 负责人
- 截止时间
- 子任务拆解
- 当前状态

#### Step 5\. 路由

- 大群收到立项卡
- Jerry 私聊收到内容任务卡
- Tom 私聊收到视觉任务卡

#### Step 6\. 人类 gate

发起人确认：

- 拆解是否准确
- owner 是否正确
- deadline 是否正确

#### Step 7\. 执行与回执

系统记录：

- 谁已接受
- 哪个子任务已完成
- 当前整体状态

#### Step 8\. 状态查看

用户查询：

`@loom 给我看下这次热点响应现在到哪了`

系统输出：

- 一张 `status-snapshot`
- 当前完成情况
- 卡点位置
- 需要决策的下一步

#### Step 9\. 记忆更新

系统写回：

- 热点响应默认拆解方式
- 该团队对快节奏协作的广播偏好
- 哪类任务默认发给谁

### 14.4 这个模板验证什么

- 协作意图是否能被稳定收敛
- 路由是否清楚
- 公海 / 私聊分流是否有效
- 状态概览是否减少工具切换

---

## 15\. 需求模板 B：会议结果进入执行流

### 15.1 场景

团队开完一场外部合作会议，需要把会议结果自动进入执行流。

### 15.2 输入来源

这条流程不要求人工手动输入会议内容。

输入来源可以直接来自：

- 自动纪要
- action items
- transcript
- 录屏链接
- summary email

### 15.3 用户输入示例

`@loom 把刚才这场合作会整理进执行流，先给我看下要落的三件事。`

### 15.4 系统流程

#### Step 1\. Ingest

系统接入：

- summary
- action items
- 参会人
- 录屏链接
- transcript

#### Step 2\. Parse

系统识别：

- 哪些是 decision
- 哪些是 follow-up
- 哪些需要 owner
- 哪些需要继续约会

#### Step 3\. 建立协调实例

创建一个 `Coordination Issue`：

- `type = meeting_followup`
- `source = meeting result`

#### Step 4\. 生成执行摘要

输出 `summary-digest`：

- 本次会议结论
- 三件待推进事项
- 每件事建议 owner
- 原始纪要与录屏入口

#### Step 5\. 人类 gate

用户确认：

- 哪些事项真正进入执行流
- owner 是否准确
- 哪些内容需要对团队广播

#### Step 6\. 落盘与路由

系统把确认后的事项写成：

- task
- decision
- reminder

并发给相关 owner。

#### Step 7\. 持续跟进

后续用户查询：

`@loom 上周那个交易所合作现在卡在哪`

系统输出：

- 会议结论
- 当前任务状态
- 卡点位置
- 下一步所需决策

#### Step 8\. 记忆更新

系统写回：

- 会后执行摘要的偏好格式
- 哪类会议默认需要 follow-up
- 哪些动作应该先私发再公示

### 15.5 这个模板验证什么

- 外部会议智能层能否顺利接入
- 会议结果能否进入团队状态流
- 团队是否减少“会开完了再手工整理”的负担

---

## 16\. 系统如何长出来

`loom+` 的基线版本只提供稳定框架。

后续形态由团队使用推动演化，演化方向包括：

- 团队级广播规则
- 私聊 / 公海分流习惯
- 个人提醒偏好
- 摘要和快照的常用格式
- 常见协作流模板
- 角色之间的默认协作模式

团队每天怎样使用它，决定了 `loom+` 后面会长成什么样。

---

## 17\. 当前阶段不追求的内容

- 完整对标 Slack
- 完整对标 Notion
- 功能全景图
- 大规模自动协商网络
- Personal Daemon 全量落地
- 泛化到所有团队都能直接使用

当前阶段只需要把框架定住，把最小流程跑稳，把可升级和可部署能力立住。

---

## 18\. MVP 成功标准

`loom+ v0` 的成功标准聚焦在稳定框架和可重复协作。

### 最小成功标准

1. 协作意图进入系统后，可以创建 `Coordination Issue`
2. 每个实例能沿着固定状态流推进
3. 系统能留下工件和回执
4. 团队成员可以通过一句话查看状态
5. 热点响应流和会议跟进流至少各跑通一次
6. 新增需求进入系统后，可以在不破坏底座的情况下完成修改和重新部署

---

## 19\. 最终定义

**`loom+ v0` 是一个复用 `Deepflow` 底座能力的团队协作框架。它以协调实例为运行单位，用状态机、工件、gate、路由和记忆层组织团队协作，并通过日常使用逐步长成团队自己的协作系统。**

# 执行摘要

**loom+ v0 执行摘要**

**目标** `loom+` 当前阶段的任务，是把团队协作这个领域收敛成一个可运行、可升级、可重新部署的框架。它复用 `Deepflow` 已经验证过的底座能力，不直接照搬编程流程。重点在于稳定性、工件沉淀、状态推进、人类 gate 和后续可维护性。

**一句话定义** `loom+` 是一个团队协作框架。它接住协作意图，创建协调实例，推进状态流，留下工件和回执，并在日常使用中逐步学习团队自己的协作方式。

**当前要解决的问题**

- 团队成员在聊天、任务、文档、会议和提醒之间反复切换
- 协作推进依赖人类手动做上下文路由
- 状态变化、广播和跟进分散在不同工具中
- 需求一旦变化，系统容易失稳
- 早期“AI 员工”式通用聊天能力无法自然收敛成稳定协作系统

**基线框架** `loom+` 的基线框架分成固定层和可变层。

固定层包括：

- 协调实例 `Coordination Issue`
- 协作状态机
- 工件系统
- 人类 gate
- 路由规则
- 记忆更新规则

可变层包括：

- 团队协作习惯
- 个人偏好
- 广播风格
- 快照模板
- 提醒阈值
- 常用流程模板

**最小运行单位** `Coordination Issue` 是最小运行单位。一次协作意图进入系统后，只要会触发状态变化、路由、提醒、广播、摘要或确认动作，就创建一个协调实例。

典型来源：

- 启动一个跨职能任务
- 跟进一个热点响应
- 查询某个项目状态
- 从会议结果生成后续动作
- 改动某个负责人或 deadline
- 生成团队或个人简报

**四个核心动作**

- `Ingest`：接住一句话意图、会议结果、外部报警、状态变化
- `Route`：判断该创建什么实例、通知谁、发哪里、需不需要确认
- `Render`：按需输出卡片、快照、摘要、回执、风险提示
- `Learn`：把团队规则、个人偏好、输出习惯写回记忆层

**基线状态流**

- `received`
- `parsed`
- `planned`
- `routed`
- `pending_confirmation`
- `executing`
- `receipted`
- `summarized`
- `closed`

**基线工件**

- `intent-brief`
- `coordination-card`
- `routing-receipt`
- `status-snapshot`
- `execution-receipt`
- `summary-digest`
- `rule-update`

**必须保留的人类 gate**

- 建任务确认
- 改 owner 确认
- 改 deadline 确认
- 发群公告确认
- 固定会议 action items
- 固定 decision
- 修改团队级规则

**稳定性约束**

- 任何重要协作动作都挂在某个 `Coordination Issue` 下推进
- 状态变化必须写入系统工件或数据库
- 关键动作必须留下回执
- 广播必须克制，大群只承载结论、公示、风险和必要状态变化
- 关键动作统一卡 gate
- 每个阶段支持单独重跑
- 系统支持回放，能看到意图、路由、停点和结果
- 记忆更新需要有明确来源，不能无限自发演化

**需求模板 A：热点响应协作流** 适用场景：`crypto AI` 团队跟进一个热点，短时间内组织内容、视觉和定调。  
输入示例：  
`@loom 今晚跟进这个热点，明天上午前给我一版英文 thread、一版中文解读、一张封面图，Jerry 写内容，Tom 做图，我来定调。`

系统流程：

- 接住意图
- 创建协调实例
- 生成协调卡
- 路由给对应 owner
- 发起人确认拆解
- 执行并留下回执
- 用户可随时看当前 snapshot
- 写回团队的热点响应习惯

这条模板验证：

- 意图能否稳定收敛
- 路由是否清楚
- 公海 / 私聊分流是否有效
- 状态概览是否减少工具切换

**需求模板 B：会议结果进入执行流** 适用场景：团队开完一场外部合作会议，需要把会议结果自动进入执行流。  
输入来源直接来自：

- 自动纪要
- action items
- transcript
- 录屏链接
- summary email

用户只需要说： `@loom 把刚才这场合作会整理进执行流，先给我看下要落的三件事。`

系统流程：

- 接入会议结果
- 创建协调实例
- 提炼 decisions 和 follow-ups
- 生成执行摘要
- 用户确认哪些事项真正进入执行流
- 写成 task / decision / reminder 并路由
- 后续可随时查询当前卡点
- 写回团队的会后执行习惯

这条模板验证：

- 外部会议智能层能否顺利接入
- 会议结果能否进入团队状态流
- 团队是否减少会后手工整理负担

**系统如何长出来** `loom+` 的基线版本先提供稳定框架。后续形态通过团队日常使用逐步长出来。主要变化来源包括：

- 团队级广播规则
- 私聊 / 公海分流习惯
- 个人提醒偏好
- 摘要和快照常用格式
- 常见协作流模板
- 角色间默认协作模式

**当前阶段不追求**

- 完整对标 Slack
- 完整对标 Notion
- 功能全景图
- 大规模自动协商网络
- Personal Daemon 全量落地
- 泛化到所有团队都能直接使用

**MVP 成功标准**

- 协作意图进入系统后，可以创建 `Coordination Issue`
- 每个实例能沿固定状态流推进
- 系统能留下工件和回执
- 团队成员可以通过一句话查看状态
- 热点响应流和会议跟进流至少各跑通一次
- 新增需求进入系统后，可以在不破坏底座的情况下完成修改和重新部署
